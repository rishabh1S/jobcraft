@AGENTS.md

# Package Manager
Use yarn instead of npm. Always use equivalent yarn commands.

## Examples
- `yarn add <pkg>` not `npm install <pkg>`
- `yarn` not `npm install`
- `yarn dev` not `npm run dev`